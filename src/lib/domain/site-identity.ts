export type SiteIdentity = Readonly<{
  name: 'SUBSOLO';
  slogan: 'O ruído passa. O que importa fica.';
  timezone: 'America/Sao_Paulo';
}>;

export const siteIdentity: SiteIdentity = Object.freeze({
  name: 'SUBSOLO',
  slogan: 'O ruído passa. O que importa fica.',
  timezone: 'America/Sao_Paulo',
});
