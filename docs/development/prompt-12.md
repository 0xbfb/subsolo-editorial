# Prompt 12 — Orquestração de publicação com n8n

Implementados core funcional, seis workflows sanitizados, persistência SQL, locks, idempotência, dead-letter, alertas, dry-run e reconciliação. A validação runtime do n8n/PostgreSQL depende de Docker externo.
